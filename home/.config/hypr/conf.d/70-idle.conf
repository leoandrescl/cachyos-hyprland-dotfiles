# Start hypridle when installed; no-op otherwise.
exec-once = ~/.config/hypr/scripts/start-idle.sh

# Manual lock shortcut outside system submap.
bind = $mainMod CTRL, L, exec, pidof hyprlock >/dev/null || hyprlock
